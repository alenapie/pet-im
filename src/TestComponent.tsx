"use client";

import { Button } from "@/shared/ui";

export const TestComponent = () => {
  return (
    <div>
      <Button variant="text" onClick={() => {}}>
        1232
      </Button>
    </div>
  );
};
